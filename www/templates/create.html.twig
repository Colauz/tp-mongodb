{% extends "base.html.twig" %}

{% block title %}Ajouter un manuscrit{% endblock %}

{% block content %}
    <div class="container">
        <h1>Ajouter un nouveau manuscrit</h1>

        {% if message %}
            <div class="alert alert-danger">{{ message }}</div>
        {% endif %}

        <form method="post" action="create.php">
            <div class="mb-3">
                <label for="cote" class="form-label">Cote</label>
                <input type="text" class="form-control" id="cote" name="cote" required>
            </div>

            <div class="mb-3">
                <label for="titre" class="form-label">Titre</label>
                <input type="text" class="form-control" id="titre" name="titre" required>
            </div>

            <div class="mb-3">
                <label for="auteur" class="form-label">Auteur</label>
                <input type="text" class="form-control" id="auteur" name="auteur">
            </div>

            <div class="mb-3">
                <label for="siecle" class="form-label">Siècle</label>
                <input type="text" class="form-control" id="siecle" name="siecle">
            </div>

            <button type="submit" class="btn btn-success">Enregistrer</button>
            <a href="index.php" class="btn btn-secondary">Annuler</a>
        </form>
    </div>
{% endblock %}